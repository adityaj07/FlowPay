{
  "version": 2,
  "builds": [
    {
      "src": "index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "src/(.*)", "dest": "index.ts" }],
  "rewrites": [{ "source": "/api/(.*)", "destination": "/src/index.ts" }]
}
